import { ReportDetailComponent } from './report-detail-component';

describe('ReportDetailComponent', () => {
  it('should create an instance', () => {
    expect(new ReportDetailComponent()).toBeTruthy();
  });
});
