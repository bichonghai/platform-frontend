import { ReportListComponent } from './report-list-component';

describe('ReportListComponent', () => {
  it('should create an instance', () => {
    expect(new ReportListComponent()).toBeTruthy();
  });
});
